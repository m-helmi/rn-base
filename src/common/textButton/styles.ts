import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    // paddingHorizontal: 5,
    paddingBottom: 4,
  },
  content: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  text: {
    fontSize: 14,
  },
  text_container: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});
